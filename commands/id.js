module.exports.config = {
    name: "id",
    description: "Returns a user's ID",
    usage: "{USER}",
    ownerOnly: false,
    adminOnly: false,
    argsMinimum: 1
}

module.exports.run = (client, message, args) => {

}