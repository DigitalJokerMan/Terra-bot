module.exports.config = {
    name: "eval",
    description: "Runs a function",
    usage: "{FUNCTION}",
    ownerOnly: true,
    adminOnly: false,
    argsMinimum: 0
}

module.exports.run = (client, message, args) => {

}